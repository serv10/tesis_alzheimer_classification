/* import type { SelectedImage, UploadStatus } from "@interfaces/interface";
import { ImageUploadingItem } from "@components/ImageUploadingItem";

interface Props {
  selectedFile: SelectedImage;
  uploadStatus: UploadStatus;
  progress: number;
}

export const ImageUploading = ({
  selectedFile,
  uploadStatus,
  progress,
}: Props) => {
  return (
    <div className="w-full">
      <h5 className="text-gray-500 font-semibold">Uploading file</h5>
      <ImageUploadingItem
        selectedImage={selectedFile}
        uploadStatus={uploadStatus}
        progress={progress}
      />
    </div>
  );
};
 */
