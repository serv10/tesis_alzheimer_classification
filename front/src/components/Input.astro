---
const { placeholder, id, class: className, type } = Astro.props;
---

<input
  type={type}
  id={id}
  class=`shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block w-full p-2.5 ${className}`
  placeholder={placeholder}
  required
/>
